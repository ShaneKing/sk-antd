import {Dir, SIZE} from './Const';

let DEFAULT = {
  //AntD Default
  COL_SPAN: 8,
  FORM_LABEL_COL: {span: 8},
  FORM_LAYOUT: Dir.Horizontal,
  FORM_WRAPPER_COL: {span: 16},
  GRID_GUTTER: 0,
  SIZE: SIZE.Default
};

export default DEFAULT;
